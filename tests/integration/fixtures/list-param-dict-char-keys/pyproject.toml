[project]
name = "list-param-dict-char-keys"
version = "0.1.0"
dependencies = ["pymcu-stdlib", "pymcu-micropython"]

[tool.pymcu]
board = "arduino_uno"
frequency = 16000000
sources = "src"
entry = "main.py"
stdlib = ["micropython"]
