[project]
name = "ds18b20"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pymcu-stdlib>=0.1.0",
]

[tool.pymcu]
chip      = "atmega328p"
frequency = 16000000
sources   = "src"
entry     = "main.py"
