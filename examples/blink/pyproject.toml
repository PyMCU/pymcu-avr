[project]
name = "blink"
version = "0.1.0"
dependencies = ["pymcu-stdlib"]

[tool.pymcu]
chip = "atmega328p"
frequency = 16000000
sources = "src"
entry = "main.py"

[tool.pymcu.flash]
programmer = "avrdude"
port = "/dev/cu.usbmodem14101"  # uncomment and set your serial port
