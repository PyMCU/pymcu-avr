[project]
name = "slice-ops"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "whisnake-stdlib>=0.1.2a5",
    "whipsnake>=0.1.0a27"
]

[tool.whip]
chip = "atmega328p"
frequency = 16000000
sources = "src"
entry = "main.py"
